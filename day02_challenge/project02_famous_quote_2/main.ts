//famous person Quote with variable
let famousPerson: string = "Quaid-e-Azam"; 
let message: string = (`${famousPerson}, once said, “Think a hundred times before you take a decision, but once that decision is taken, stand by it as one man.”`);
console.log(message)
